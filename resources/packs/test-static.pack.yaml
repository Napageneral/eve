id: test-static
name: Test Static Pack (No Backend Required)
version: 1.0.0
category: test
tags: [test, static]

description: |
  Simple test pack using only static snippets - no backend calls required.

flexibility: high
total_estimated_tokens: 500

slices:
  - name: TEST_CONTEXT
    retrieval: static_snippet
    params:
      text: |
        This is test context.
        It contains sample conversation data for testing.
        No backend retrieval needed.
    estimated_tokens: 500
    encoding_explainer: plain_text
    why_include: "Test purposes only"

