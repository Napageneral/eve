{
  "name": "eve-intelligence-layer",
  "version": "1.0.0",
  "type": "module",
  "description": "Eve Intelligence Layer - Context + Prompt Assembly + Agent System",
  "private": true,
  "scripts": {
    "start": "bun run server.ts",
    "dev": "bun --watch server.ts",
    "start:old": "bun run context-engine/server.ts"
  },
  "dependencies": {
    "cron-parser": "^5.4.0"
  }
}
